{
  "case": "pr-11-branch",
  "description": "SQL injection via fmt.Sprintf on user-supplied query param — must catch as security/high (or above).",
  "findings": [
    {
      "path": "cmd/api/search.go",
      "start_line": 19,
      "end_line": 20,
      "category": "security",
      "min_severity": "high",
      "description": "SQL injection: name from r.URL.Query() interpolated into query string; must use parameterized query"
    }
  ]
}
